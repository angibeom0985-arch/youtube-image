# Environment Configuration
# Copy this file to .env or .env.local for local development

# Node Environment (development or production)
NODE_ENV=development

# Server Configuration
PORT=3003

# Allowed CORS Origins for production (comma separated)
# Leave commented for development (will use localhost)
# ALLOWED_ORIGINS=https://youtube-image.money-hotissue.com,https://www.youtube-image.money-hotissue.com

# Google Gemini AI API Key (optional - users can input via UI)
GEMINI_API_KEY=your_gemini_api_key_here

# 사용 방법:
# 1. 이 파일을 .env 또는 .env.local로 복사하세요
# 2. your_gemini_api_key_here를 실제 Google AI Studio에서 발급받은 API 키로 교체하세요
# 3. .env 파일은 절대로 Git에 커밋하지 마세요 (.gitignore에 추가됨)