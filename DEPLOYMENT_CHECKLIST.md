# 🚀 배포 체크리스트

## ⚠️ UI/UX 깨짐 방지 가이드

### 문제 증상
- 웹사이트 UI가 갑자기 깨짐
- CSS 스타일이 적용되지 않음
- JavaScript 로딩 실패

### 주요 원인
1. **개발 서버 중단** - 로컬 개발 시 서버가 백그라운드에서 종료됨
2. **캐시 문제** - 브라우저/CDN 캐시가 최신 코드를 반영하지 못함
3. **빌드 실패** - Vercel 배포 중 빌드 에러 발생

---

## ✅ 배포 전 체크리스트

### 1. 로컬 테스트
```bash
# 개발 서버 시작 (백그라운드)
npm run dev

# 브라우저에서 테스트
# http://localhost:3000

# 빌드 테스트
npm run build

# 빌드 결과 미리보기
npm run preview
```

### 2. 코드 커밋 전
- [ ] TypeScript 에러 없음 확인
- [ ] 콘솔 에러 없음 확인
- [ ] 주요 기능 테스트 완료
- [ ] CSS 스타일 정상 확인

### 3. Git 커밋
```bash
git add .
git commit -m "설명적인 커밋 메시지"
git push origin main
```

### 4. Vercel 배포 확인
- [ ] Vercel 대시보드에서 배포 상태 확인
- [ ] 빌드 로그 에러 확인
- [ ] 배포 완료 후 사이트 접속 테스트

---

## 🔧 문제 해결 방법

### UI가 깨진 경우

#### 로컬 개발 환경
```bash
# 1. 개발 서버 재시작
Ctrl + C  # 기존 서버 중단
npm run dev

# 2. 브라우저 캐시 클리어
Ctrl + Shift + R  # 강력 새로고침
```

#### 프로덕션 환경 (Vercel)
```bash
# 1. 강제 재배포
git commit --allow-empty -m "Force redeploy"
git push origin main

# 2. Vercel 대시보드에서 "Redeploy" 클릭

# 3. 브라우저 캐시 클리어
Ctrl + Shift + Delete -> 캐시된 이미지 및 파일 삭제
```

### 개발 서버가 자꾸 중단되는 경우
```bash
# Git 명령 실행 시 별도 터미널 사용
# 또는 백그라운드 실행 스크립트 사용 (아래 참조)
```

---

## 🛠️ 개발 서버 안정화 설정

### Vite 설정 (vite.config.ts)
```typescript
server: {
  port: 3000,
  host: '0.0.0.0',
  hmr: {
    overlay: true,    // 에러 오버레이 표시
    clientPort: 3000  // HMR 포트 고정
  }
}
```

### Vercel 캐시 설정 (vercel.json)
```json
{
  "headers": [
    {
      "source": "/index.html",
      "headers": [
        { "key": "Cache-Control", "value": "public, max-age=0, must-revalidate" }
      ]
    },
    {
      "source": "/assets/(.*)",
      "headers": [
        { "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }
      ]
    }
  ]
}
```

---

## 📱 배포 후 확인 사항

### 필수 테스트
- [ ] 홈페이지 로딩
- [ ] API 키 입력/저장
- [ ] 페르소나 생성 (3개 이하로 테스트)
- [ ] 영상소스 생성 (3장 이하로 테스트)
- [ ] 이미지 다운로드
- [ ] 모바일 반응형 확인

### 성능 확인
- [ ] 페이지 로드 시간 < 3초
- [ ] Lighthouse 점수 확인
- [ ] 콘솔 에러/경고 없음

---

## 🚨 긴급 복구 절차

### 1단계: 이전 버전으로 롤백
```bash
# Vercel 대시보드에서
# Deployments -> 이전 정상 버전 -> "Promote to Production"
```

### 2단계: 로컬에서 문제 재현
```bash
git log --oneline  # 최근 커밋 확인
git checkout <이전-정상-커밋-해시>
npm run dev  # 정상 작동 확인
```

### 3단계: 문제 커밋 수정
```bash
git checkout main
# 문제 파일 수정
git add .
git commit -m "Fix: UI 깨짐 문제 해결"
git push origin main
```

---

## 💡 예방 조치

### 1. 자동 테스트 추가 (향후)
```bash
npm run test  # 테스트 실행
npm run lint  # 코드 린팅
```

### 2. Git Hooks 설정
```bash
# pre-commit: 커밋 전 자동 빌드 체크
# pre-push: 푸시 전 자동 테스트
```

### 3. 정기 점검
- 주 1회: 의존성 업데이트 확인
- 월 1회: 전체 기능 테스트
- 분기 1회: 성능 최적화 점검

---

## 📞 도움 요청

문제가 계속되면:
1. 브라우저 콘솔 에러 스크린샷
2. Vercel 빌드 로그
3. 네트워크 탭 확인 (F12 -> Network)
4. 문제 재현 단계 상세 기록
